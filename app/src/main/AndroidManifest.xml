<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.homework">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.ACTION1" />

                <category android:name="android.intent.category.CATEGORY1" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.ACTION2" />

                <category android:name="android.intent.category.CATEGORY2" />
            </intent-filter>
        </activity>
        <activity android:name=".DisplayActivity">
            <intent-filter>
                <action android:name="android.intent.action.ACTION3" />

                <category android:name="android.intent.category.CATEGORY3" />
            </intent-filter>
        </activity>
    </application>

</manifest>